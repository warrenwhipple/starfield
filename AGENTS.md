# Project Agents

**Starfield** — A minimal single-player real-time space conquest game. TypeScript, Pixi.js, DES engine.

## Tooling

- **Vite** — Dev server (`npm run dev`), build (`npm run build`), preview (`npm run preview`). Vanilla TS + ESM.
- **Vitest** — Unit tests (when present).

## Design

Full spec: [DESIGN.md](./DESIGN.md)
